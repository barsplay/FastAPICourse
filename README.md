# FastAPICourse — API для изучения иностранных слов

## 📌 Описание проекта

**FastAPICourse** — это backend‑приложение на **FastAPI**, предназначенное для изучения иностранных слов с помощью карточек. Проект реализует:

* аутентификацию и авторизацию пользователей;
* роли пользователей (обычный пользователь / администратор);
* управление карточками слов;
* отслеживание прогресса изучения;
* асинхронную работу с базой данных через SQLAlchemy.
---

## 🛠️ Стек технологий

* **Python 3.10+**
* **FastAPI**
* **Uvicorn**
* **SQLAlchemy (async)**
* **Pydantic**
* **JWT (OAuth2 Password Flow)**
* **SQLite / PostgreSQL** (зависит от конфигурации)

---

## 📂 Структура проекта

```
FastAPICourse/
│
├── requirements.txt
└── app/
    ├── main.py            # Точка входа приложения
    ├── database.py        # Подключение и инициализация БД
    ├── models.py          # SQLAlchemy модели
    ├── schemas.py         # Pydantic схемы
    ├── crud.py            # CRUD‑операции
    ├── auth.py            # Логика аутентификации и JWT
    │
    ├── routers/
    │   ├── auth.py        # Роуты аутентификации
    │   ├── cards.py       # Роуты карточек слов
    │   └── progress.py    # Роуты прогресса пользователя
```

---

## 🔐 Аутентификация и роли

Используется **JWT‑аутентификация**:

* `/auth/register` — регистрация пользователя
* `/auth/login` — получение access‑token

### Роли:

* **Пользователь** — может просматривать карточки и сохранять прогресс
* **Администратор** — может создавать, изменять и удалять карточки

---

## 📘 Карточки слов (`/cards`)

Функциональность:

* Получение списка карточек
* Просмотр отдельных карточек
* Создание / редактирование / удаление карточек (только администратор)

Каждая карточка содержит:

* иностранное слово
* перевод

---

## 📊 Прогресс (`/progress`)

Позволяет:

* сохранять прогресс изучения слов
* получать статистику по изученным карточкам

Прогресс привязан к конкретному пользователю.

---

## 🚀 Запуск проекта

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd FastAPICourse
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск сервера

```bash
uvicorn app.main:app --reload
```

## 📑 Документация API

После запуска приложение будет доступно по адресу:

* **Swagger UI:** `/docs`
* **ReDoc:** `/redoc`

## Администратор
```
Login: admin
Password: admin123
```
